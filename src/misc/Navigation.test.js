import React from 'react';
import { render, getByLabelText, fireEvent } from '@testing-library/react';

describe('Navigation', () => {
  describe('about on click link', () => {
    it.todo('Navigates to `Artem Pizza Page`');
  });
});
